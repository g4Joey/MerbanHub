package com.merbancapital.backend.service;

/**
 * DocumentService (DB-backed) has been disabled in this branch because documents
 * are stored on disk in the OCR folders. The original implementation used the
 * database and is intentionally commented out to avoid accidental DB access.
 *
 * If you later re-enable DB-backed behavior, restore this class and its
 * repository wiring.
 */
public class DocumentService {
    // Intentionally left blank (DB-backed DocumentService disabled).
}
