version: '3.8'

services:
  ocr-service:
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    volumes:
      - "${SCAN_FOLDER}:/app/scans"
      - "${OUTPUT_FOLDER}:/app/output"
      - "./logs:/app/logs"
    depends_on:
      db:
        condition: service_healthy

  db:
    image: postgres:14
    env_file:
      - .env
    volumes:
      - db_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD","pg_isready","-U","ocr","-d","ocrdb"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  db_data: {}
